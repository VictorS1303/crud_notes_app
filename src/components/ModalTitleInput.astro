---
const {minlength, maxlength, className, id} = Astro.props
---

<input type="text" class="modal-title-input" id={id} placeholder="Enter title" minlength={minlength} maxlength={maxlength}>


<style>
    .modal-title-input
    {
        font-family: inherit;
        font-size: 15px;
        position: absolute;
        top: 10%;
        left: 50%;
        transform: translateX(-50%);
        width: 85%;
        padding: 10px 15px;
        border-radius: inherit;
        background-color: #fff;
    }
</style>